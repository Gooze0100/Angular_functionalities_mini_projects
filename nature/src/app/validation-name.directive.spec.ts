import { ValidationNameDirective } from './validation-name.directive';

describe('ValidationNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationNameDirective();
    expect(directive).toBeTruthy();
  });
});
